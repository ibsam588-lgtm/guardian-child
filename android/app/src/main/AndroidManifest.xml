<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Networking -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Foreground service - keeps app alive in background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Required so SirenService can play audio as a mediaPlayback foreground service on API 29+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Ambient listening: parent can remotely activate the child mic to
         hear what's happening around the device. API 34+ requires the
         microphone foreground-service subtype. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Block-screen popup. Full-screen-intent notifications are the only
         reliable way to bring an activity to the foreground from a
         background timer on API 29+, which is what the block screen relies on
         when the child is already inside YouTube/etc. SYSTEM_ALERT_WINDOW
         is listed as a belt-and-suspenders fallback for devices where the
         full-screen intent is downgraded to a heads-up notification. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Restart after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Battery info & optimization exemption -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- App usage stats - required for UsageStatsManager (child app monitoring) -->
    <!-- This is a special permission: user must grant it via Settings > Apps > Usage access -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Call log and SMS reading for parental monitoring -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:label="GuardIan Child"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Block screen shown when a child's app has exceeded its daily limit -->
        <activity
            android:name=".AppBlockedActivity"
            android:exported="false"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/LaunchTheme" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Foreground monitoring service -->
        <service
            android:name=".MonitorService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Siren alarm service — runs even when the app is closed.
             mediaPlayback type allows audio on API 29+ without the 3-minute shortService kill limit.
             stopWithTask=false keeps it alive after the user swipes the app away. -->
        <service
            android:name=".SirenService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false" />

        <!-- Ambient listen service — records mic audio in 5s chunks,
             uploads each chunk to Firestore, and stops itself when
             the parent issues listen_stop. microphone type is required
             on API 34+ for background mic access. -->
        <service
            android:name=".ListenService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:stopWithTask="false" />

        <!-- Boot receiver to restart monitoring -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Browser activity tracking accessibility service -->
        <!-- NOTE: requires prominent disclosure + Play Console video before publishing to production -->
        <service
            android:name=".BrowserMonitorService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Firebase Cloud Messaging — subclass handles siren at native layer so
             the alarm plays even when the app is backgrounded or fully killed. -->
        <service
            android:name=".GuardianMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Required on Android 11+ so getLaunchIntentForPackage can find
             installed launcher apps. Without this, the installed-apps picker
             in the parent app is missing apps like YouTube, Chrome, etc. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

</manifest>
